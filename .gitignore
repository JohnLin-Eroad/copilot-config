# Runtime state — never commit
notion-vault-state.json
command-history-state.json
mcp-oauth-config/
session-state/
jb/
logs/
restart
obsidian-sync.log
obsidian-sync.error.log

# Stale local backups — not meant for git history
backups/

# OS
.DS_Store

# Secrets (real config lives in ~/.copilot/, not here)
*.secret
.env
