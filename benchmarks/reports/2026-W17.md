# Benchmark Report — 2026-W17

**Date:** 2026-04-28
**Overall score:** 4.615 / 5.0
**vs last week:** +0.265 (2026-W16: 4.35)
**Experiment branch:** none (weekly-experimenter produced no branch this week)

## Score Summary

| Category | Score | vs W16 | Notes |
|---|---|---|---|
| Code Generation | 5.0/5 | ±0.0 | Perfect: record, port, tests — reference quality maintained |
| Context Retrieval | 4.5/5 | +0.9 | Domain model entity files now in vault — major retrieval improvement |
| Security Review | 4.7/5 | +0.1 | All 3 found; precision improved (resource leak correctly downgraded to noise) |
| Planning | 5.0/5 | +0.1 | Blast radius improved; 8 edge cases; Terraform + alerting callout |
| Learning Retention | 3.0/5 | ±0.0 | NEUTRAL — no experiment branch this week |
| **Overall** | **4.615/5** | **+0.265** | Strongest score to date |

## Code Generation

**Score: 5.0/5** — All 5 dimensions at maximum.

- **Correctness (5):** Factory guard validates negative km and includes value in exception message. `plus()` returns new `VehicleOdometer` — immutability preserved. Port returns `Optional<VehicleOdometer>` correctly.
- **Hexagonal compliance (5):** `VehicleOdometer` in `domain.odometer` (pure stdlib — no framework imports). `OdometerRepository` in `application.port.out` (output port pattern). Zero cross-layer leakage.
- **Java 21 idioms (5):** Java `record` for immutable value object. `Optional` used correctly in port. Factory method pattern. No `@Autowired` field injection anywhere.
- **Javadoc completeness (5):** Class-level Javadoc on both types. All public methods: `@param`, `@return`, `@throws` where applicable. Port interface includes package-level context note.
- **Test quality (5):** 4 required test cases covered. Zero value is separate test. Negative case asserts both exception type AND message content. Addition test additionally verifies immutability of originals. AssertJ fluent assertions. `@DisplayName` throughout.

## Context Retrieval

**Score: 4.5/5** — Significant improvement from W16's 3.6.

**Files fetched (6):**
1. `03 - Architecture/eroad-platform-architecture.md` — multi-cloud arch, EKS, event-driven pipeline
2. `03 - Architecture/sovereign-v2-spec.md` — AI-governed execution platform, Vanguard roadmap
3. `02 - Domain Models/README.md` — entity registry (7 entities, relationship diagram)
4. `02 - Domain Models/Driver.md` — HOS records, fleet membership, soft delete pattern
5. `02 - Domain Models/Vehicle-Machine.md` — primary asset entity, telemetry split, org scoping
6. `02 - Domain Models/Fleet.md` — access control grouping, authorisedMachineIds/DriverIds

**Per-question accuracy:**
- ✅ EROAD architecture: AWS+Azure multi-cloud, EKS microservices, legacy GlassFish ASG, ATOM→ESP→Kinesis event pipeline. Well covered.
- ✅ Sovereign role: AI-governed execution platform (governance + orchestration + delivery layers), builds toward Vanguard (EROAD's next-gen enterprise platform). Well covered.
- ❌ Java/Spring Boot version: NOT in vault. Correctly stated as gap — "check `copilot-instructions.md` or codebase `pom.xml`." No hallucination.
- ✅ Domain entities: Driver, Machine/Vehicle, Fleet, Organisation, Trip, Event, Device — all documented with owning services, key fields, relationships. **This was the W16 gap — now fully resolved.**

**What changed:** The `02 - Domain Models/` directory with 7 entity markdown files was added to the brain vault. This lifted domain entity coverage from partial (W16: mentioned incidentally in architecture docs) to comprehensive (W17: dedicated entity files with full field/relationship specs).

**Remaining gap:** Java 21 + Spring Boot 3.4 is not brain-vaultable from public info — it lives in the codebase's `pom.xml`. Consider adding a "Technology Stack" note file to the vault.

## Security Review

**Score: 4.7/5** — All 3 planted vulnerabilities found. Precision slightly improved.

**Findings map:**

| Finding | OWASP | Severity | Verdict |
|---|---|---|---|
| SQL string concatenation in `executeQuery()` | A03 | CRITICAL | ✅ TRUE_POSITIVE |
| `owner_email` in response body (no masking) | A02 | HIGH | ✅ TRUE_POSITIVE |
| `"wrong password: " + request.getPassword()` in 401 | A09 | HIGH | ✅ TRUE_POSITIVE |
| `/search` has no authentication | A01 | HIGH | ✅ BONUS REAL (not planted) |
| Connection/Statement/ResultSet not closed | N/A | MEDIUM | ⚠️ NOISE (code quality, not security) |

- **Recall: 1.0** — All 3 planted vulnerabilities found.
- **Precision: 0.80** — 4 real issues out of 5 flagged. Resource leak correctly downgraded to code-quality note (not counted as security finding). W16 precision was 0.75.
- **Severity accuracy: 1.0** — All correctly CRITICAL or HIGH.
- **Formula:** `((1.0×0.5) + (0.80×0.3) + (1.0×0.2)) × 5 = 0.94 × 5 = 4.70`

## Planning Quality

**Score: 5.0/5** — Slight improvement from W16's 4.9; blast radius now at maximum.

- **Domain understanding (5):** `DrivingSession` (aggregate), `ComplianceThreshold` (policy value object — carries the 11h/13h rule), `ComplianceStatus` (enum), `ComplianceEvent` (entity). Business rule placed inside domain layer correctly.
- **Hexagonal architecture (5):** Domain → Application (use case + `NotificationPort` + `ComplianceEventRepository` output ports) → Infrastructure (`SqsNotificationAdapter`, `ComplianceEventJpaAdapter`) → API (`ComplianceEventController`). Zero violations.
- **Blast radius (5):** Specific DDL (`compliance_events` table with exact columns), SQS queue + DLQ, 3 named config keys, existing `notification-service` dependency, new DB index, Terraform provisioning step, CloudWatch alarm on DLQ depth. W16 lost 0.5 here; W17 full marks.
- **Edge cases (5):** 8 edge cases identified: midnight boundary, timezone/jurisdiction differences (NZ vs ELD-regulated US), multi-fleet deduplication, clock drift fallback, rest break reset (≥30 min), retroactive trip amendment, jurisdiction-based threshold config, ELD overlap avoidance.
- **Dependency ordering (5):** 10-step topological order: domain model → ports → use case → DB migration → adapters → event listener → controller → integration tests → SQS infra → alerting. No circular dependencies.
- **Acceptance criteria (5):** 5 Given/When/Then ACs with 30s SLA, state assertions, feature-flag scenario (AC5), and time-boundary scenario (AC4). Measurable and automatable.

## Learning Retention

**Score: 3.0 / 5.0 — NEUTRAL**

`experiments/2026-W17.md` does not exist. The weekly-experimenter agent ran but produced no experiment branch for W17. Per rubric: score = 3 (NEUTRAL).

The learning retention benchmark will produce meaningful scores from W18 onward, once the weekly-experimenter begins creating branches targeting specific capability improvements.

**Recommended experiment targets for W18** (based on remaining gaps):
- `context-retrieval`: Add a "Technology Stack" file to the vault (Java 21, Spring Boot 3.4, Gradle, PostgreSQL) to close the Java/Spring gap and push context retrieval toward 5.0.
- `security-review`: Experiment with structured output format for findings (JSON schema) to test whether it reduces noise (resource leak category).

## Trends

After two weeks of data, the trajectory is clearly positive. Code generation (5.0) and planning (5.0) are at ceiling. The context retrieval jump (+0.9 this week, 3.6→4.5) demonstrates that brain vault enrichment directly translates to benchmark score improvement — the addition of the `02 - Domain Models/` entity files was a concrete, measurable gain. Security review is near-ceiling (4.7). The main lever left is: (1) close the Java/Spring technology stack gap in the vault, and (2) start generating experiment branches to make learning retention meaningful.

## Recommended Actions

1. **[HIGH PRIORITY] Add `04 - Technology Stack/stack.md` to eroad-brain vault** — Document Java 21, Spring Boot 3.4, Gradle, PostgreSQL, AWS SDK v2. Will push context retrieval from 4.5 → ~4.8 by answering the Java/Spring question from brain data.
2. **[MEDIUM] Configure weekly-experimenter to run** — W17 and W16 both show NEUTRAL (3.0) for learning retention. Even a small experiment targeting context retrieval or security precision would activate this 10% weight.
3. **[LOW] Security: structured findings format** — Experiment with asking the security agent to output findings in a JSON schema (severity, owasp_ref, code_location, fix). May reduce noise and push precision to 0.9+, targeting security score of 4.9.
